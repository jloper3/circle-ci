machine:
  python:
    version: 2.7.9
dependencies:
  post:
    - pip install awscli
test:
  override:
    - jq '.' *.cf
deployment:
  production:
    branch: master
    tag: /release-.*/
    owner: jloper3
    commands:
      - VPC=PROD ./deploy.sh
